<%= form_for(@interest_point) do |f| %>
  <% if @interest_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interest_point.errors.count, "error") %> prohibited this interest_point from being saved:</h2>

      <ul>
      <% @interest_point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :location %><br />
    <%= f.text_field :location %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
